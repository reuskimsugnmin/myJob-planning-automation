---
description: 노션 기획 티켓을 인입·파싱해 engagement 폴더와 00-project-brief.md 생성 (워크플로우 1~2)
argument-hint: <노션-티켓-URL>
---

`planning-intake` 스킬을 사용해 티켓을 인입하세요. 대상: $ARGUMENTS

URL이 비어 있으면 사용자에게 노션 티켓 URL을 요청하세요. 티켓에 연결된 페이지가 탐색 예산(약 8~10개)을 넘으면 전체 목록을 제시하고 **어디까지 탐색할지 사용자에게 확인**하세요. 완료 후 슬러그·프로젝트 유형·핵심 요건·미해결 의사결정·미상 항목을 보고하고, **확정된 3단계 경로에 따라 다음 단계를 제안**하세요(레거시 개선→`/domain-study`, 신규 기능·화면→`/reference-research`, 혼합→둘 다). 이후 파이프라인은 **3단계 리서치 → `/tech-research`(4) → `/prd`(5)** 순서임을 함께 안내하세요.
