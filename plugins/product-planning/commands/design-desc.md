---
description: Figma 스토리보드 화면에 기획 디스크립션 작성 + 디자인 동기화 (워크플로우 12)
argument-hint: [engagement-slug] [--figma-url url] [--scan|--apply]
---

`design-description` 스킬을 사용해 디자인 화면의 기획 디스크립션을 작성하고 디자인과 동기화하세요. 대상 engagement: $ARGUMENTS. `--figma-url` 로 대상 파일/페이지를, `--scan`(변경 계획만)/`--apply`(적용)로 모드를 지정하세요(미지정이면 전체).

번호 뱃지↔annotation 1:1 매칭(가장자리 거리·아이콘 인터랙션 후보 포함), 본문 형식(역할 문장 + 대괄호 섹션, Figma 노드명 금지, 인터랙티브 컴포넌트 `[Default 상태]` 명시), 2단계 검증(개수+의미 스크린샷)을 따르고, **디스크립션 내용을 실제 디자인에 동기화**한 뒤 **claim↔노드 대조표**(0건도 표로 증명)로 완료를 증명하세요. 쓰기 원시는 `figma-design` 스킬. 산출물·백업·로그는 `engagements/<slug>/design/`.

## 일시정지 규칙
- **디자인 미실현**: 대상 화면이 아직 PRD대로 만들어지지 않았으면(스토리보드 선행 필요) 경고하고 `/storyboard` 를 먼저 권한다.
- **claim↔노드 불일치**: 대조표에 ✗가 있으면 sync-policy대로 수정하거나, 노드 특정 불가 등 SKIP 조건이면 `SKIP: [이유]` 기록 후 사용자에게 항목별 확인.
- **`--scan` 모드**: 변경 계획만 보고하고 적용은 사용자 검토 후 `--apply` 로.
- **모호한 정책**: 의미가 불명확하면 자의 해석하지 말고 사용자 확인.
